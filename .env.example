# Ancient Free Will Database - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# LOCAL DEVELOPMENT
# =============================================================================

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration (Local PostgreSQL)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=ancient_free_will_db
POSTGRES_USER=free_will_user
POSTGRES_PASSWORD=free_will_password

# Qdrant Configuration (Local Qdrant)
QDRANT_HOST=localhost
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334

# Application Configuration
LOG_LEVEL=INFO
EMBEDDING_MODEL=gemini-embedding-001
EMBEDDING_DIMENSIONS=3072

# =============================================================================
# PRODUCTION DEPLOYMENT (Supabase + Qdrant Cloud)
# =============================================================================
# For Supabase, use these settings:
# POSTGRES_HOST=db.[your-project-ref].supabase.co
# POSTGRES_PORT=5432
# POSTGRES_DB=postgres
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=[your-supabase-password]

# For Qdrant Cloud, use these settings:
# QDRANT_HOST=[your-cluster-id].qdrant.io
# QDRANT_HTTP_PORT=6333
# QDRANT_API_KEY=[your-qdrant-api-key]

# For production deployment on Render, set all these environment variables
# in the Render dashboard. See DEPLOYMENT.md for complete instructions.
