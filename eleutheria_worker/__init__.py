"""EleutherIA Temporal worker.

Runs against the platform's Temporal cluster. The package is importable without
Temporal infrastructure running — only `eleutheria_worker.main` actively
connects.
"""

__version__ = "0.1.0"
